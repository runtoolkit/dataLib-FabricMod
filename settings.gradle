pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url = "https://maven.fabricmc.net/"
        }
        mavenCentral()
    }
}

rootProject.name = "datalib-fabricmod"

include("core")
include("command")
include("log")
include("main")
